Todo:


Parser:
- Lifetimes Problem mit root fixxen

Lexer
- Lexer um input_len erweitern und peek ohne strlen bei jedem Aufruf implementieren
- Zeile und Spalte tracken, nicht nur pos für bessere Errors
- TokenKind Namen prefixen, z.B. TOKEN_IDENT, TOKEN_IF
- "_" als token löschen 
- Number Lexing verbessern: nur einen Punkt erlauben, "12.34.56" als Fehler melden, "." nicht aus versehen als Float behandeln
- string und char escapes implementieren
- keyword erkennung verbessern (eigene hashmap? oder array)
- alle printfs entfernen (nur durch lexer result fehler melden)
- Token Werte besser speichern: entweder Slice aus dem Input mit start/len oder klare ownership mit Kopie.

AST
- Ownership festlegen: Wer besitzt char* Namen, Typen, Literals und AST-Knoten? => passende free methoden!! 
- Konstruktoren für ast knoten erstellen 
- bool statt in für flags
- spans bei expr stmt oder top level nodes anhängen für bessere errors später
- Literal werte nicht nur als char* speichern sondern eventuell schon als Type enum? (oder erst später??)
- namen einheitlich machen
- Enum Werte prefixen z.B. UNARY_NEG/UNARY_NOT statt Neg/Not
- Überlegen ob ASTNOde wirklich alle varianten braucht oder ob top level decl stmt und expr typen besser sind
- Arena-Allocator für später (und an memory free denken?)


+ basis nach parser / ast speichern eventuell noch type checking, sodass ich eine variante als compiler und eine als interpreter bauene kann


